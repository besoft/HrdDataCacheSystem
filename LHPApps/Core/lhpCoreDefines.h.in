#ifndef __lhpCoreDefines_h
#define __lhpCoreDefines_h

#if @LHP_BUILD_DLL@
# define I_SHARED
#endif

//--------------------------------------------------------------------------
// For WIN32 dynamic linking
//--------------------------------------------------------------------------
#if defined(_WIN32) && defined(I_SHARED)
# if defined(LHP_CORE_EXPORTS)
#  define LHP_CORE_EXPORT __declspec( dllexport ) 
# else
#  define LHP_CORE_EXPORT __declspec( dllimport ) 
# endif
#else
# define LHP_CORE_EXPORT
#endif

#endif // __lhpCoreDefines_h